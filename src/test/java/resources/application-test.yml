spring:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        show_sql: true
        format_sql: true
  datasource:
    url: jdbc:h2:mem:users;testdb
    driver-class-name: org.h2.Driver
    username: user
    password: root123*
  profiles:
    active: test












